# Django Secret Key (generate a new one for production)
SECRET_KEY=02^!kcghk$45hflo(xr%glute34^^pgk7ezi=7y9l(44q(n-p1

# Debug mode (True for development, False for production)
DEBUG=True

# Allowed hosts (comma-separated list)
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Static and Media URLs
STATIC_URL=/static/
MEDIA_URL=/media/

VIRTUAL_ENV=./venv
PATH=./venv/Scripts;%PATH%

# Celery Configuration - Enable synchronous processing when Redis is not available
CELERY_TASK_ALWAYS_EAGER=True

# Email settings (used in settings.py)
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Security Headers
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False
SECURE_SSL_REDIRECT=False
USE_SSL_PROXY=False

# Cookie Security
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_AGE=3600

# Redis Cache (uncomment and set if using Redis)
# REDIS_URL=redis://localhost:6379/1

# Rate Limiting
RATELIMIT_ENABLE=True
