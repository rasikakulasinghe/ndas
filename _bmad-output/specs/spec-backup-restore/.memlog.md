---
topic: Institutional Admin & Super Admin Data Backup/Restore
updated: 2026-09-16T19:02
---

- (direction) User request: add data backup option for institutional admin and super admin; each backup includes media, database information, and other attachments; include restore option; suggest further professional options
- (note) Grounding: architecture.md line 765 already flags 'DB backup strategy' as a deferred nice-to-have gap -- this spec activates that deferred item
- (note) Grounding: no existing backup/restore code in repo; institution/ app already implements institutional-admin vs super-admin tenant scoping (middleware.py, context_processors.py) that this feature must reuse rather than parallel
- (note) Grounding: no Celery/Redis client packages in requirements.txt and no CELERY settings in ndas/settings.py -- async task runner is not yet present in this project, a real gap for background job execution
- (capability) CAP-1 Manual backup creation: institutional admin or super admin can trigger creation of a backup archive containing their scoped DB records, media files, and attachments. Success: archive contains DB export + media + manifest; institutional admin scoped to own institution, super admin system-wide or selected institution(s)
- (capability) CAP-2 Backup manifest and integrity verification: every archive includes a manifest with schema/app version, included institution(s), record counts, checksums. Success: restore of an archive with mismatched schema version, altered checksum, or corrupted contents is rejected before any data is modified
- (capability) CAP-3 Restore with automatic pre-restore safety snapshot: admin can restore from a prior backup archive; system auto-snapshots current state before applying. Success: restore completes only after preview/confirmation; a failed or cancelled restore can be reverted using the pre-restore snapshot
- (capability) CAP-4 Scoped access control for backup/restore: actions permitted per existing institutional-admin/super-admin roles, reusing institution/ app's scoping. Success: institutional admin attempting backup/restore outside their institution is denied
- (capability) CAP-5 Async execution with progress and notification: backup/restore run as background jobs so large media sets do not block the request; progress visible, notification on completion/failure. Success: a multi-GB job can be started, admin navigates away, later sees completion status without HTTP timeout
- (capability) CAP-6 Backup history and retention management: admins view list of past backups (who, when, size, scope), can download or delete, optional retention policy auto-prunes old backups. Success: backup list shows metadata; delete removes archive; enabled retention policy auto-removes backups past threshold
- (capability) CAP-7 Audit logging of backup/restore actions: every create/download/restore action recorded with acting user, timestamp, scope, consistent with existing UserActivityMiddleware/UserTrackingMixin. Success: audit log shows who created/restored a given backup and when
- (constraint) Must reuse institution/ app's existing tenant-scoping and permission model (middleware.py, context_processors.py) rather than building a parallel authorization mechanism
- (constraint) Backup archives containing PHI must never be stored under or served via a publicly-accessible static/media path; download requires authenticated, permission-checked access only
- (constraint) Backup/restore must run as background/async jobs, not synchronous request-response, given existing file size limits (2GB video, 100MB doc) multiplied across many records
- (constraint) Restore must never apply directly to production data without an automatic pre-restore snapshot and an explicit confirmation step
- (constraint) A restore archive is itself a file upload and must pass the same MIME/type/size validation as other uploads (python-magic pattern) before being trusted
- (constraint) Backup/restore trigger endpoints must be rate-limited per existing django_ratelimit pattern to prevent abuse via repeated large-job triggers
- (decision) Non-goal: real-time/continuous replication or point-in-time recovery -- this is snapshot-based backup, not an HA/replication solution
- (decision) Non-goal: cross-institution data merging during restore -- a restore applies only within the scope it was created for
- (decision) Non-goal: automatic offsite/cloud storage upload (S3/Azure) is not committed for this iteration -- captured as an open question, not a requirement
- (decision) Non-goal: backup/restore of deployment configuration (Django settings, environment secrets) is out of scope -- already covered separately by scripts/switch_env.py; this feature covers application data (DB + media/attachments) only
- (decision) Success signal: super admin triggers a full-system backup and institutional admin triggers a scoped backup of only their institution's data; both can later restore through a preview-confirm flow with automatic pre-restore snapshot; backup/restore actions appear in the audit trail -- demonstrable end-to-end on staging with production-representative media volume, no Django shell/manual DB intervention required
- (assumption) Assumed backup format is a single compressed archive per backup event (not separate DB-only and media-only downloads), for handling simplicity given the manifest requirement
- (assumption) Assumed 'attachments' in the user's request refers to the Document/Image/Video file types already defined in FILE_UPLOAD_LIMITS, not a new attachment type
- (assumption) Assumed local-disk storage under a non-public path is the storage target for v1; offsite/cloud storage is an open question, not a hard requirement, since the user asked only to 'suggest options'
- (assumption) Assumed background job execution needs an async task runner (e.g. Celery); confirmed via grep that no Celery/Redis packages or CELERY settings currently exist in this project, so this is a new architectural dependency requiring confirmation, not an existing pattern to reuse
- (question) Should scheduled/automatic backups (e.g. nightly) be in scope for v1, or manual on-demand only with scheduling deferred to a later phase?
- (question) What retention policy defaults should apply (keep last N, max age, max total storage), and does the institutional admin control their own institution's retention, or does only super admin set retention policy system-wide?
- (question) Is offsite/cloud storage (S3, Azure Blob) required for this iteration given PHI disaster-recovery expectations, or is local-disk storage acceptable for v1 with offsite deferred to Phase 2?
- (question) Should backup archives be encrypted at rest by default given they contain PHI, and if so what key-management approach (Django SECRET_KEY-derived, separate KMS, per-institution key)?
- (question) Should this feature introduce Celery (+ broker) as the project's first async task runner, or use a lighter mechanism (management command via OS cron/Windows Task Scheduler, threading) given no async infra exists yet?
- (question) Should institutional admins be able to restore only into their own institution's live data, or also preview/import into a sandbox before committing to a live restore?
- (question) What is the maximum acceptable backup/restore duration or dataset size before this needs chunking/streaming rather than a single archive job?
- (note) Spawned companion professional-options.md: catalog of enterprise/professional backup features (storage & transport, security & compliance, scheduling & automation, reliability & verification, observability & UX, governance & retention) beyond the v1 capabilities, as candidate future capabilities responsive to the user's request to 'suggest further options'
- (event) Self-validate Pass 1 (coherence, rules 1-6 & 8): PASS -- all 7 capabilities have intent+success, intents are WHAT not HOW, all 6 constraints bend design, 4 non-goals present, success signal is concrete/demonstrable, CAP IDs unique, prose lean
- (event) Self-validate Pass 2 (preservation): PASS -- every load-bearing claim from the user's request and grounding context (media/DB/attachments inclusion, restore, professional options, institution scoping reuse, missing async infra, FILE_UPLOAD_LIMITS validation pattern, rate limiting, audit trail, switch_env.py precedent) traced into SPEC.md or professional-options.md; no wrapper-only content dropped
- (event) Spec finalized: SPEC.md + professional-options.md written for spec-backup-restore; 7 capabilities, 6 constraints, 4 non-goals, 4 assumptions, 7 open questions
- (decision) Open question resolved: manual on-demand backups only for v1; scheduled/automatic backups deferred out of scope (already reflected in professional-options.md's Scheduling & automation group)
- (decision) Open question resolved: retention policy is set system-wide by super admin only, not per-institution by institutional admins
- (question) New (narrower) open question: what should the default system-wide retention values be -- keep last N backups, max age in days, or both?
- (decision) Open question resolved: local-disk storage only for v1; offsite/cloud storage (S3/Azure) deferred to Phase 2, remains in professional-options.md
- (decision) Open question resolved: NO encryption at rest for backup archives in v1. User explicitly wants the archive to be manually browsable/visualized: database information exported in human-readable form (not an opaque binary-only dump), media files organized into clear folders/subfolders rather than a flat blob
- (note) Grounding: video/models.py and patients/models.py already partition media on disk via get_institution_video_path / get_institution_attachment_path as '{institution_slug}/videos/...' and '{institution_slug}/attachments/...' -- CAP-1 can require the archive preserve this existing institution/type folder structure rather than invent a new one
- (constraint) Because backups are unencrypted at rest, the non-public-storage-path and authenticated-permission-gated-access constraints are the primary protective controls for PHI in the archive -- both must be enforced without exception
- (decision) Open question resolved: no automatic/scheduled triggering of backup or restore in v1 -- every backup and restore action is always explicitly initiated by an admin (reinforces the scheduling decision above)
- (note) The specific async execution mechanism for admin-triggered long-running jobs (Celery, background thread, OS-scheduled management command) is HOW not WHAT -- deferred to bmad-architecture; narrowed by the no-automatic-scheduling decision, which removes the need for a recurring job scheduler like Celery beat specifically
- (decision) Open question resolved and CAP-3/CAP-4 scope changed: restore is a SUPER-ADMIN-ONLY operation. Institutional admins can create and manage backups of their own institution (CAP-1, CAP-6) but cannot trigger a restore under any circumstance -- this supersedes the original CAP-3 intent which assumed institutional admins could also restore their own institution's data
- (decision) Open question resolved: build streaming/chunked I/O for backup and restore from the start, no artificial dataset-size cap -- avoids a rework once an institution's data volume grows
- (event) Self-validate Pass 1 (coherence, rules 1-6 & 8) on updated spec: PASS -- CAP-1/CAP-3/CAP-4/CAP-6 updated in place (same IDs, no renumbering) to reflect human-browsable-archive and super-admin-only-restore decisions; all 7 capabilities still have intent+success, 7 constraints all bend design (incl. new streaming and no-encryption compensating-control constraints), 7 non-goals present, success signal concrete, prose lean
- (event) Self-validate Pass 2 (preservation) on updated spec: PASS -- all 6 answered open questions traced into SPEC.md (scheduling, retention ownership, offsite storage, encryption/browsability, async-mechanism deferral, restore-scope) as constraints/non-goals/capability edits; 1 narrower open question (retention default values) correctly carried forward since it was not resolved to a concrete number; no wrapper-only content dropped
- (event) Spec updated: SPEC.md re-derived for spec-backup-restore after open-questions walkthrough -- 7 capabilities (CAP-1, CAP-3, CAP-4, CAP-6 revised), 7 constraints, 7 non-goals, 3 assumptions, 1 open question remaining (default retention values)
