---
topic: Fix bookmark_edit write-access IDOR and getPatientList invalid related-name FieldError
updated: 2026-08-25T08:45
---

- (direction) Source: two items pulled from deferred-work.md (2026-08-25 triage of codebase-review-2026-08-23.md) as the two highest-priority unresolved bugs, bundled into one spec at user's request.
- (capability) CAP-1 intent: only a Bookmark's owner or a superuser can view or submit patients/views.py:bookmark_edit (GET/POST /bookmarks/edit/<pk>/); currently (patients/views.py:1691-1714) it fetches Bookmark.objects.get(id=pk) with zero ownership/institution/superuser check before rendering the form and before saving POSTed changes -- any authenticated user can view AND overwrite any other user's bookmark by guessing/incrementing pk (write-access IDOR, worse than the already-fixed view-only bookmark_manager_user IDOR).
- (capability) CAP-1 success: unauthenticated request redirects to login (existing @login_required behavior, unchanged); authenticated non-owner non-superuser GET or POST to bookmark-edit/<pk>/ for a bookmark they do not own returns HttpResponseForbidden (403) before any query result is rendered or any save occurs, mirroring the bookmark_manager_user pattern at patients/views.py:1673-1679 (username/superuser check + logger.warning on block); owner and superuser requests behave exactly as before (unchanged 200/redirect flow); a regression test covers both the blocked non-owner case and the unaffected owner/superuser case.
- (capability) CAP-2 intent: ndas/custom_codes/custom_methods.py's getPatientList() DIAGNOSED, DX_GMA_ABNORMAL, and DX_GMA_NORMAL branches (lines 621, 632, 634) query the correct GMAssessment related_name (gm_assessments, per patients/models.py:866) instead of the invalid gmassessment, so these three branches return correctly filtered patients instead of raising a Django FieldError.
- (capability) CAP-2 success: confirmed live and reachable, not dead code -- patients/views.py:220-248's patient_manager view maps filter_type=diagnosed/gma_abnormal/gma_normal query params directly to PtStatus.DIAGNOSED/DX_GMA_ABNORMAL/DX_GMA_NORMAL and calls getPatientList(pts_type, ...), so any authenticated user hitting those filter URLs triggers the FieldError today (500) whenever the queryset is evaluated; after the fix, GET requests to patient-manager with each of the three filter_type values return 200 with a patient list correctly scoped by gm_assessments__diagnosis_conclusion; a regression test exercises all three branches (mirrors the existing test_patient_manager_diagnosed_filter pattern noted in deferred-work.md for the sibling DX_NORMAL bug fixed in spec-fix-medical-data-correctness).
- (constraint) CAP-1: do not change bookmark_edit's URL, template (bookmark/edit.html), or the success-path behavior for legitimate owners/superusers -- add only the ownership guard, no redesign.
- (constraint) CAP-1: do not add @require_http_methods or @ratelimit to bookmark_edit or bookmark_manager_user in this spec -- that is a separate, already-identified deferred-work item (bookmark_manager_user missing @require_http_methods/@ratelimit) and stays out of scope here to keep this fix narrow.
- (constraint) CAP-2: do not touch the DX_NORMAL branch (patients/views.py-adjacent custom_methods.py:623-631) -- it was already fixed and is marked status:done in spec-fix-medical-data-correctness; do not redesign getPatientList's classification scheme, only correct the three invalid field references to match the working gm_assessments pattern already used at line 628.
- (constraint) CAP-2: the related_name fix must exactly match the pattern already proven correct at custom_methods.py:628 (gm_assessments__diagnosis_conclusion) -- no alternate query construction.
- (non_goal) Not fixing: HINE 3-bucket UI gap (score 60-73), get_latest_hine/gma_assessment tiebreaker, virus-scan stub, Excel-export Celery task institution leak, InstitutionScopedManager missing safe helper, or the ~180-item mechanical findings batch (group 5) -- all remain separate deferred-work.md entries for future specs.
- (non_goal) Not adding rate-limiting or HTTP-method restrictions to any bookmark view in this spec.
- (assumption) Ownership model for CAP-1 mirrors bookmark_manager_user exactly: owner (request.user == selected_bm.owner) or is_superuser may proceed; no separate institution-scoping needed since Bookmark has no institution field and bookmark_manager_user's existing pattern uses only owner/superuser.
- (event) Spec drafted 2026-08-25 from deferred-work.md entries plus direct code inspection of patients/views.py:1673-1714 and ndas/custom_codes/custom_methods.py:563-644, confirming both bugs are live and reachable (not dead code).
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS. Both capabilities carry intent+success; constraints each rule out real scope creep; non-goals explicit; success signal ties to named regression tests; CAP IDs unique within this spec folder; prose lean.
- (event) Self-validate pass 2 (preservation): PASS. Both deferred-work.md source claims (bookmark_edit ownership gap, getPatientList invalid related_name in DIAGNOSED/DX_GMA_ABNORMAL/DX_GMA_NORMAL) landed in Why + their respective capability. Wrapper-only content dropped: prior-spec forensic rationale (pre-existing-vs-introduced-by-which-commit provenance for the getPatientList bug) -- narrative color, not load-bearing for building or testing the fix.
- (event) SPEC.md rendered at _bmad-output/specs/spec-fix-bookmark-edit-idor-and-patientlist-bug/SPEC.md. 2 capabilities, 0 companions, 0 open questions, 1 assumption.
