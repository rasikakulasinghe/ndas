- source_spec: none
  summary: Fix auth/permission bypasses (bookmark IDOR, CSRF-unsafe GET mutation, open redirect, bookmark delete-permission bug)
  evidence: Split from "fix all issues in codebase-review-2026-08-23" — distinct root cause (access control) from the cross-tenant data-scoping fix, independently shippable per the review's suggested fixation order group 2. See codebase-review-2026-08-23.md items patients/views.py:1672-1681, referral/views.py:548-572, institution/middleware.py:136-138, ndas/custom_codes/delete_helpers.py:47-50.

- source_spec: none
  summary: Fix medical-data-correctness bugs (DX_NORMAL Q-object short-circuit, HINE "normal" threshold mismatch, birth-weight validation never running, HINE .last() vs .first() bug)
  evidence: Split from "fix all issues in codebase-review-2026-08-23" — distinct root cause (clinical classification logic) from the cross-tenant data-scoping fix, independently shippable per the review's suggested fixation order group 3. See codebase-review-2026-08-23.md items ndas/custom_codes/custom_methods.py:623-628, patients/views.py:2824-2830, patients/models.py:471-478, patients/models.py:748-751.

- source_spec: none
  summary: Fix security-sanitization gaps (sanitize-then-unescape XSS bypass, reverse-tabnabbing missing rel=noopener, virus-scan stub always reports clean)
  evidence: Split from "fix all issues in codebase-review-2026-08-23" — distinct root cause (input/output sanitization) from the cross-tenant data-scoping fix, independently shippable per the review's suggested fixation order group 4. See codebase-review-2026-08-23.md items ndas/custom_codes/validators.py:42-61, ndas/custom_codes/sanitization.py:37-61, patients/models.py:1931-1937.

- source_spec: none
  summary: Batch-fix remaining ~180 mechanical findings (N+1 queries, missing db_index, bare except-Exception error-swallowing, duplicate sanitize_filename/clean_profile_picture helpers, dead code, admin/UI bugs) across all 8 apps
  evidence: Split from "fix all issues in codebase-review-2026-08-23" — lower-risk, high-volume cross-cutting cleanup best tackled as its own batch(es) per the review's suggested fixation order group 5, rather than folded into the higher-risk security/correctness fixes tackled first.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-cross-tenant-data-leaks.md`
  summary: reports/tasks.py's generate_excel_report_task (Celery async Excel export) never passes institution into ExcelReportGenerator.generate(), so it silently defaults to unfiltered/all-institutions
  evidence: Surfaced independently by both the blind-hunter and verification-gap review layers during the cross-tenant-leak fix. Confirmed via repo-wide search that the task has no .delay()/.apply_async() call site today (unreachable), so it's out of scope for this story, but it reintroduces the exact leak this spec fixes the moment it's wired up — should be fixed before that task is ever dispatched.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-cross-tenant-data-leaks.md`
  summary: InstitutionScopedManager has no safe helper (e.g. for_institution_or_none()) to stop future call sites from repeating the for_institution(None)-returns-unfiltered footgun this spec just patched at multiple call sites
  evidence: Raised by the blind-hunter review layer — the fix pattern (`for_institution(institution) if institution else X.objects.none()`) is now duplicated ad hoc across referral/views.py (x3) and the Excel generator with no shared abstraction or guard, so the next call site can silently reintroduce the same cross-tenant leak. Architectural improvement, not appropriate to bundle into this narrow bugfix (spec's Never section forbids touching InstitutionScopedManager itself).

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-auth-permission-bypasses.md`
  summary: patients/views.py's bookmark_edit has no ownership/institution check at all — the exact same IDOR class as bookmark_manager_user, but for editing another user's bookmark content, not just viewing the list
  evidence: Found incidentally by the verification-gap review layer while tracing consumers of the Bookmark ownership model this spec fixed. bookmark_edit (patients/views.py:1685-1699) fetches `Bookmark.objects.select_related(...).get(id=pk)` with no owner/staff/superuser/institution check before rendering the edit form and saving changes — any authenticated user can edit any other user's bookmark by guessing/incrementing pk. Worth prioritizing soon since it's arguably worse than the view-only IDOR just fixed (write access, not just read).

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-auth-permission-bypasses.md`
  summary: bookmark_manager_user still lacks @require_http_methods and @ratelimit, unlike sibling bookmark views (e.g. bookmark_delete)
  evidence: Raised by the blind-hunter review layer — pre-existing gap, not introduced by this spec's ownership-guard fix, but inconsistent with CLAUDE.md's documented view pattern and the rate-limiting already applied to every other bookmark write/read endpoint in this file.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-medical-data-correctness.md`
  summary: ndas/custom_codes/custom_methods.py's getPatientList uses the invalid field name `gmassessment__diagnosis_conclusion` (correct related_name is `gm_assessments`) in the PtStatus.DIAGNOSED, DX_GMA_ABNORMAL, and DX_GMA_NORMAL branches (lines ~621, 630, 632) — this 500-errors whenever exercised with data. No test exercises these three branches, so the bug goes uncaught by CI.
  evidence: Found by the implementation subagent while fixing the same invalid-field bug on the adjacent DX_NORMAL line (necessary there because fixing the `and`→`|` short-circuit meant Django now actually evaluates that Q object). The other three branches have the identical field-name bug but were left untouched per this spec's Never boundary (marked read-only in the Code Map). Confirmed live via `test_patient_manager_diagnosed_filter` and similar test failures, and independently reconfirmed by running the same tests against the pre-this-spec baseline commit (78ba8aa) — identical failure list, so this is 100% pre-existing, not introduced by this spec. All three review layers (blind-hunter, edge-case-hunter, verification-gap) independently flagged this same bug.
  RESOLVED by: commit 93681b3 "fix: close bookmark_edit write-access IDOR and getPatientList FieldError" (2026-08-25, spec-fix-bookmark-edit-idor-and-patientlist-bug.md) — all three branches corrected to `gm_assessments__diagnosis_conclusion`, with regression tests. This ledger entry was left unannotated at the time; backfilled 2026-09-01.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-medical-data-correctness.md`
  summary: The HINE assessment manager's 3-bucket UI filter (normal/moderate/significant) has a gap for scores 60-73 ("Mild Abnormality" per the model) — now that 'normal' correctly requires score>73, those records fall into neither 'normal' nor 'moderate' (which starts at <60) when filtered, though they still count toward the displayed 'total', so the three stat buckets no longer sum to the total
  evidence: Raised by the blind-hunter and edge-case-hunter review layers. This is the direct, known consequence of this spec's explicit Never boundary ("do not redesign the 3-bucket scheme to match the model's 4-category scheme... only the normal threshold mismatch is being fixed") — correctly scoped out of this bugfix, but worth a follow-up to add a 4th "mild" bucket or otherwise reconcile the UI with `HINEAssessment.severity_category`. The 'moderate'/'significant' boundary at score=40 also still disagrees with the model's own `elif self.score > 40` cutoff (a separate, pre-existing mismatch, also out of scope here).

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-medical-data-correctness.md`
  summary: Patient.get_latest_hine_assessment() (and get_latest_gma_assessment()) has no tiebreaker for multiple records sharing the exact same date_of_assessment — .order_by('-date_of_assessment').first() is nondeterministic between calls in that case
  evidence: Raised by the edge-case-hunter review layer. Pre-existing characteristic of these two methods (not introduced by this spec — getRC now calls get_latest_hine_assessment() instead of duplicating a broken query, but the tiebreaker gap already existed for every other caller of these methods). Out of scope per this spec's Never boundary (both methods are marked read-only/do-not-touch in the Code Map). Low real-world likelihood (requires two assessments at the identical timestamp) but a one-line fix (add `-id` as a secondary order_by key) whenever these methods are next touched.

- source_spec: none
  summary: Attachment._schedule_virus_scan() (patients/models.py:1928-1934) always marks uploads is_scanned=True, scan_result='clean' with no real scan — needs genuine antivirus integration (e.g. ClamAV), not a quick fix
  evidence: Split out of the "security-sanitization gaps" deferred group after investigation showed this is not a narrow bugfix like its two siblings (XSS unescape-order, reverse-tabnabbing). Attachment.get_download_url() and can_be_previewed both gate on is_safe_to_view, which (per the winning/later of two duplicate is_safe_to_view definitions in the same file) requires is_scanned AND scan_result=='clean' — nothing else in the codebase ever transitions a file out of 'pending'. "Honestly" fixing the stub (leaving scan_result='pending' until a real scanner runs) would therefore block every attachment download/preview app-wide with no immediate fix path, since there is no ClamAV/AV dependency or service wired into this project at all. Requires its own scoped spec: pick and integrate a scanner (sync or async — video attachments up to 2GB make synchronous scanning during upload impractical, implying a Celery task + a "scanning" intermediate UI state), decide the interim UX for pending/infected files, and only then flip the stub over. User explicitly deferred this (2026-08-24) rather than accept the download/preview breakage from a same-day fix.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-bookmark-edit-idor-and-patientlist-bug.md`
  summary: patients/tests/test_views.py's entire PatientManagerTestCase class (test_patient_manager_diagnosed_filter, dx_normal_filter, dx_abnormal_filter, new_filter, high_risk_filter, pagination, etc.) is already broken independent of this spec's fix — verified by running it standalone against both the pre-fix and post-fix code: it fails with `KeyError: 'pts'` (and related `assertTemplateUsed`/label mismatches), not on assertIn assertions. patient_manager (patients/views.py) exposes its filtered/paginated list under context key `patients_page_obj`, not `pts`, and renders `patients/manager.html`, not `patients/patient/manager.html` — the whole test class predates a refactor of one or both and was never updated to match.
  evidence: Surfaced by this spec's review (blind-hunter and verification-gap layers both independently ran the test and got KeyError, not an assertIn failure — confirmed against baseline commit cd6626946e80656aabbd5ba7670c157eb7340ece, where the same errors already exist). patient_dx_normal/patient_dx_abnormal additionally lack real GMAssessment/HINEAssessment/DevelopmentalAssessment fixtures despite comments claiming otherwise (lines ~60, ~76), so even once the context-key/template mismatch is fixed, those two tests still won't pass without also fixing their fixtures. A self-contained PatientManagerGMAssessmentFilterTest was added instead (this spec) to prove the getPatientList related-name fix, independent of this whole pre-existing class. Needs its own follow-up: fix the context key and template path expected by PatientManagerTestCase, then attach real assessment fixtures to patient_dx_normal/patient_dx_abnormal.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-bookmark-edit-idor-and-patientlist-bug.md`
  summary: Bookmark._validate_bookmarked_object's and bookmarked_object's model_mapping (patients/models.py ~2222-2231, ~2266-2275) maps bookmark_type='Video' to ("patients", "Video"), but the Video model actually lives in the `video` app — apps.get_model raises LookupError, silently swallowed by the broad `except Exception`, so Bookmark validation never actually checks that a Video-type bookmark's object_id exists.
  evidence: Surfaced by this spec's blind-hunter review layer while inspecting fixtures for the new BookmarkEditSecurityTest, which had used bookmark_type='Video' with a non-existent object_id=1 and only "passed" because this pre-existing bug swallowed the validation (confirmed via the "Could not validate bookmarked object: App 'patients' doesn't have a 'Video' model" warning in test output). Fixed in this spec's own tests by switching the fixture to bookmark_type='Patient' (whose mapping is correct), but the underlying model_mapping bug in patients/models.py is untouched and out of scope here — needs a one-line fix (`("video", "Video")`) plus its own regression test.
  RESOLVED by: `_bmad-output/implementation-artifacts/spec-fix-gma-timeline-and-video-bookmark-mapping.md` (2026-09-01) — fixed the app label in both dicts (now factored into one shared `Bookmark.MODEL_MAPPING` constant) and added `patients/tests/test_models.py::BookmarkVideoMappingTest`. That fix also uncovered and closed a second, broader bug: the deliberately-raised `ValidationError` was itself being swallowed by the same broad `except Exception`, for every bookmark_type, not just Video — see the entry below.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-bookmark-edit-idor-and-patientlist-bug.md`
  summary: bookmark_edit (patients/views.py), like its sibling bookmark_manager_user (already tracked above), has no @require_http_methods or @ratelimit, unlike bookmark_delete and most other CRUD views in this file.
  evidence: Raised by this spec's blind-hunter review layer. Pre-existing gap (bookmark_edit had zero decorators of this kind before or after this spec's ownership-guard fix), inconsistent with CLAUDE.md's documented view pattern and the rate-limiting already applied to sibling bookmark write endpoints. Deliberately out of scope for this spec (Never boundary: "must not add @require_http_methods or @ratelimit to bookmark_edit or bookmark_manager_user").

- source_spec: none
  summary: Fix real IDOR/cross-tenant issues surfaced by the 2026-08-25 whole-app bmad-review — referral thread access control checks institution membership instead of participant identity, reports/views.py delete_report/report_history have no ownership check, institution/views.py protected_media_view skips the isolation check for logo paths
  evidence: Split from "fix all one by one as planned" (whole-app review follow-up) — distinct root cause (broken authorization/ownership checks) from the sanitization and institution-scoping fixes, independently shippable as group 2 of the user's stated priority order. Findings: referral/views.py:211-219 (_thread_panel_response) and :296-304 (referral_reply) authorize by institution membership only, not by from_clinician/to_clinician identity; reports/views.py:264-297 (delete_report) has no ownership check before os.remove, and reports/views.py:210-259 (report_history) lists every user's report filenames with no per-user scoping; institution/views.py:920-930 (protected_media_view) only checks the institution-slug match for 'videos'/'attachments' path segments, not 'logo', so cross-institution logo access is unguarded.

- source_spec: none
  summary: Fix shared XSS/injection surfaces from the 2026-08-25 whole-app bmad-review — sanitize_text_input's dangerous-protocol strip is bypassable via whitespace/control-char-obfuscated schemes, and Excel exports (reports/utils/excel_generator.py, problemlist/views.py) don't escape leading =/+/-/@ in free-text cells (formula injection)
  evidence: Split from "fix all one by one as planned" — distinct root cause (input/output sanitization for XSS and spreadsheet formula injection) from the IDOR/ownership fixes, independently shippable as group 3 of the user's stated priority order. Findings: ndas/custom_codes/validators.py:67 (sanitize_text_input) uses a literal-string regex `(javascript|data|vbscript):` that a payload like `java\tscript:alert(1)` never matches; reports/utils/excel_generator.py:334-417 and :422-598 (all sheet builders) and problemlist/views.py:591-606 (problem_analysis_export) write free-text medical fields into openpyxl cells with no leading-character escaping, so a cell value starting with `=`/`+`/`-`/`@` is stored as a live formula.

- source_spec: none
  summary: Fix users app institution-scoping gap on 4 admin CRUD views (admin_user_edit, admin_user_delete, admin_user_toggle_status, admin_user_activity) plus missing update_session_auth_hash after password change
  evidence: Split from "fix all one by one as planned" — distinct root cause (institution-boundary enforcement gap on a specific sibling-view set, plus an unrelated session-invalidation bug in the same app) from the other fix groups, independently shippable as group 4 of the user's stated priority order. Findings: users/views.py:656-705 (admin_user_edit), :708-801 (admin_user_delete), :806-833 (admin_user_toggle_status), :836-853 (admin_user_activity) all use get_object_or_404(CustomUser, pk=pk) with no institution filter, unlike userView/userViewByUsername/admin_user_list/admin_activity_logs which are institution-scoped; users/views.py:280-295 (userChangePassword) never calls django.contrib.auth.update_session_auth_hash after form.save(), so a user is silently logged out on the request immediately after changing their own password.

- source_spec: none
  summary: Fix the two silent-data-loss bugs from the 2026-08-25 whole-app bmad-review — patients/timeline_utils.py's GM Assessment event block references a nonexistent gma.observation field (drops all GMA events from every patient timeline), and patients/models.py's Bookmark model_mapping maps bookmark_type='Video' to the wrong Django app
  evidence: Split from "fix all one by one as planned" — distinct root cause (silent data loss from an AttributeError and a wrong app-label mapping, both swallowed by broad except clauses) from the other fix groups, independently shippable as group 5 of the user's stated priority order. Findings: patients/timeline_utils.py:199-204 references gma.observation, which does not exist on GMAssessment, so the AttributeError (caught by a broad except) silently drops every GM Assessment event from every patient's timeline; patients/models.py:2222-2251 (_validate_bookmarked_object) and :2266-2284 (bookmarked_object/bookmarked_object_title) map bookmark_type='Video' to ("patients", "Video") instead of ("video", "Video") — apps.get_model raises LookupError, silently swallowed, so Video-bookmark existence validation and object resolution both silently fail. (This second bug was already independently flagged in the spec-fix-bookmark-edit-idor-and-patientlist-bug deferred entry above; this entry additionally bundles the GMA-timeline sibling bug found by the later whole-app review, and the two are being fixed together as one group per the user's stated priority order.)
  RESOLVED by: `_bmad-output/implementation-artifacts/spec-fix-gma-timeline-and-video-bookmark-mapping.md` (2026-09-01) — both bugs fixed as described, plus a second broader Bookmark-validation bug this fix uncovered (see the entry above and the new entry below on the swallowed-ValidationError fix's downstream effects).

- source_spec: none
  summary: 45 pre-existing test failures/errors (12 failures, 33 errors) surfaced for the first time now that the video/reports/users test-collection collision is fixed and `python manage.py test` (bare, no app args) can finally run to completion across all 482 collected tests, instead of crashing at collection before executing anything
  evidence: Ran `python manage.py test` after fixing the collision (this spec). None of the failures are in video/, reports/, or users/ (the only apps this spec touched — video/reports/users app suites pass 59/59 in isolation), confirming they are pre-existing and unrelated to this fix; they were simply never reached before because bare `manage.py test` aborted immediately at import time. Breakdown: (1) `tests/test_integration.py` (repo-root package) fails to import — `from patients.models import Patient, GMAssessment, Video, Attachment` — Video was moved to the `video` app and this integration test was never updated (1 error). (2) `tests/test_security.py` (repo-root package) — 11 errors, not yet triaged in detail (auth/CSRF/rate-limit/security-header/middleware tests). (3) `patients/tests/test_views.py::PatientManagerTestCase` and `DashboardTestCase` — the PatientManagerTestCase portion is the same stale context-key/template/label mismatch already tracked in the spec-fix-bookmark-edit-idor-and-patientlist-bug deferred entry above (now reconfirmed still present: ~13 failures/errors); DashboardTestCase is a newly-surfaced sibling issue in the same file — `recent_patients` missing from dashboard view context, plus 2 more dashboard assertion failures (3 total, not previously tracked). (4) `problemlist/tests.py` — 9 errors (template-rendering/500-class failures in problem_add and related views) plus 1 failure (`test_unauthenticated_access` expects the URL name `'user-login'` in the redirect but the actual login URL is `/users/login/`) — not yet triaged in detail. (5) `institution/tests/test_clinician_management.py::test_admin_cannot_create_admin_type_user` and `institution/tests/test_models.py::test_multi_institution_enabled_is_false_by_default` — 2 failures, not yet triaged. (6) `referral/tests/test_notification_bell.py` — 2 failures: both assert `b'badge'` is absent from the response, but the badge `<span>` element is always rendered with `style="display:none"` when empty rather than omitted, so the literal string 'badge' is always present regardless of count. Full output: `python manage.py test -v 1` reproduces all 45.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-video-reports-users-test-collision.md`
  summary: reports/ app has zero CRUD/view-level test coverage (only reports/tests/test_security.py exists) — unlike video/ and users/, which both now have a real CRUD suite (video/tests/test_crud.py, users/tests/test_crud.py) alongside their test_security.py
  evidence: Raised by this spec's blind-hunter review layer. reports/tests.py, deleted by this spec, was a fully empty Django boilerplate stub with no test content to preserve, so there was nothing to move — but that also means reports/ views (report_builder, download_report, delete_report, report_history, etc.) have no CRUD-style regression coverage at all. Out of scope for this narrow collision-fix spec (writing a new test suite is not a mechanical rename); worth its own follow-up, and can be combined with closing the delete_report/report_history ownership-check gaps already tracked in the "IDOR/cross-tenant issues" deferred entry above, since new ownership tests would naturally live in that new suite.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-video-reports-users-test-collision.md`
  summary: video/tests/test_crud.py's VideoTestBase.setUp sets video_file.name to a path that doesn't exist on disk, so every test in that file emits warning/error-level log noise ("Failed to get file size... WinError 2", "Video file not found", "'Video' object has no attribute 'is_new_file'") that drowns real signal in test output — video/tests/test_security.py's VideoSecurityBase avoids this via Video.objects.bulk_create(...) to bypass Video.save()'s real-storage file-size read, but the two sibling fixture bases (now both under video/tests/) have diverged instead of sharing one pattern
  evidence: Raised by this spec's blind-hunter review layer, confirmed by re-running video/tests/test_crud.py and observing the noise on every test. Pre-existing (not introduced by the rename — the same noise existed in the original video/tests.py before this spec moved it). Out of scope for this collision-fix spec since consolidating the two fixture patterns touches every test's setUp in both files, more than a mechanical move warrants; worth a small follow-up to switch VideoTestBase to the bulk_create pattern (or a shared helper) once someone is next in this area.

- source_spec: none
  summary: Batch-fix the remaining ~120 lower-risk findings from the 2026-08-25 whole-app bmad-review — missing rate limits on several mutating views (assessment_delete, bookmark_add/edit, problem_status_change, problem_action_add, admin_user_delete), boundary/edge-case bugs (HINE score-range bucket gaps, isScreeningPositive/getRC None-return branches, admin misconfigurations, dead/duplicate code), and test-coverage gaps (broken PatientManagerTestCase, video/reports test-collection collision noted separately as group 1, missing tests for numerous views) across all 9 reviewed modules
  evidence: Split from "fix all one by one as planned" — lower-risk, high-volume cross-cutting cleanup best tackled as its own batch(es) per the user's stated priority order group 6, rather than folded into the higher-risk security/data-loss fixes tackled first. Full findings list (all lenses, all fields) recorded in the 2026-08-25 whole-app bmad-review chat report and ndas_whole_app_review.json.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-idor-cross-tenant-ownership-gaps.md`
  summary: get_institution_logo_path (ndas/custom_codes/validators.py) falls back to the literal slug "pending" when an Institution has no slug assigned yet; now that protected_media_view's institution-isolation check covers logo paths (this spec), a logo uploaded while the institution is still slug-less becomes inaccessible to everyone except SUPERADMIN, including that institution's own legitimate staff
  evidence: Raised by this spec's blind-hunter review layer. Pre-existing characteristic already shared by video/attachment paths (same "pending" fallback, same isolation check, for as long as protected_media_view has existed) — not introduced by this spec, just now also true for logos. Out of scope here (a real fix needs either blocking logo upload until a slug exists, or a different pending-state access rule); worth a follow-up covering all three partitioned types together.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-idor-cross-tenant-ownership-gaps.md`
  summary: The report_owner_* session-keyed cache ownership model used by download_report (pre-existing) and now also delete_report/report_history (this spec) has two structural fragility points: (1) Django's login() cycles the session key by default (session-fixation hardening), so a user who regenerates their session after generating a report loses access to delete/see it in history with no fallback; (2) the cache backend defaults to per-process LocMemCache when REDIS_URL is unset (this repo's documented default), so under a multi-worker deployment without Redis configured, cache writes in one worker are invisible to reads in another, intermittently denying the legitimate owner
  evidence: Raised by this spec's blind-hunter review layer. Both issues are inherent to the pre-existing ownership design (download_report already had them before this spec; delete_report/report_history now inherit them for parity, which is this spec's stated goal) — not introduced by this fix, and out of scope for a narrow IDOR-parity change. A real fix means moving ownership off session-key+cache onto something durable (e.g. a DB-backed GeneratedReport model keyed by user, or at minimum falling back to request.user.pk alone) — a design change, not a bugfix.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-idor-cross-tenant-ownership-gaps.md`
  summary: reports/utils/pdf_generator.py's five assessment PDF generators (GM/HINE/DA/CDIC/GPA) write into the same MEDIA_ROOT/reports/temp/ directory as the Excel report_builder flow, using their own filename scheme (e.g. gm_assessment_{id}_{uuid}.pdf) that never registers a report_owner_* cache entry — this spec's report_history fix now explicitly excludes filenames not matching REPORT_FILENAME_PATTERN (which these don't) rather than trying to make them ownership-aware, since report_history's own UI entry point (the "View History" link on the Excel builder page) suggests it was only ever meant for that flow, and these PDFs already had their own institution-scoped download endpoints
  evidence: Traced during this spec's implementation in response to a blind-hunter finding that flagged these files as "silently disappearing" from report_history once ownership filtering was added. Confirmed via reports/views.py's own get_validated_report_path regex that these filenames were already unreachable through download_report/delete_report before this spec (format mismatch, pre-existing) — only report_history's loose .endswith() filter ever listed them, which was itself part of the leak this spec fixes. If assessment PDFs should someday be manageable through this same history/delete flow, that needs its own spec: rename them to match REPORT_FILENAME_PATTERN (or relax the pattern) and add report_owner_* writes to all five generators' call sites.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-sanitization-xss-and-formula-injection.md`
  summary: ndas/custom_codes/validators.py's sanitize_text_input() dangerous-protocol strip was hardened against whitespace/control-character obfuscation (this spec) but remains bypassable via HTML-comment obfuscation (e.g. "java&lt;!-- --&gt;script:alert(1)" after entity-decoding becomes "java<!-- -->script:alert(1)", which the protocol regex doesn't recognize as "javascript:" and the generic tag-strip regex `<(/)?([a-zA-Z][a-zA-Z0-9]*)[^>]*>` doesn't match either since an HTML comment `<!--...-->` doesn't start with a letter after `<`) or other non-whitespace delimiters between protocol-name characters
  evidence: Raised by this spec's blind-hunter review layer while reviewing the whitespace-obfuscation fix. A genuinely different obfuscation technique than the whitespace/control-character one this spec closes (per the original review finding's specific evidence, "java\tscript:" and similar). Fixing it safely needs its own care to avoid new false positives against legitimate text containing literal "<!--"-like sequences (rare in medical free text, but the fix should be scoped and tested on its own rather than folded into this narrower whitespace-obfuscation fix) — worth a dedicated follow-up: strip HTML comments (and any other non-alphanumeric noise) before the protocol-name match, not just before/after it.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-users-institution-scoping-and-session-hash.md`
  summary: The new get_institution_scoped_user_or_404() helper (and every other institution-scoping check in this codebase) keys superuser bypass off request.user.is_superuser, while institution/middleware.py's _resolve_institution_context keys institution-resolution off request.user.user_type == UserType.SUPERADMIN — a different field. Nothing enforces or tests that these two flags are guaranteed to move together for a given account
  evidence: Raised by this spec's blind-hunter review layer. Pre-existing characteristic shared by every institution-scoped view in the codebase (userView/userViewByUsername had the identical is_superuser check before this spec; this spec's helper just consolidates the existing pattern, not a new divergence). If a SUPERADMIN-user_type account is ever created without is_superuser=True (or vice versa), institution scoping in views and the middleware's own resolution logic would disagree — worth a model-level constraint or a signal ensuring the two stay in sync, or a shared is_platform_admin(user) helper that checks both consistently. Out of scope for a narrow IDOR-scoping fix.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-users-institution-scoping-and-session-hash.md`
  summary: get_institution_scoped_user_or_404() (and the pre-existing userView/userViewByUsername pattern it consolidates) scopes non-superusers by `institution=request.institution`, which matches another user with institution=None whenever the requester also has institution=None — a non-superuser staff account not yet assigned to any institution can view/edit/deactivate any OTHER not-yet-assigned account, not just ones at "their own" institution (which doesn't exist yet for them)
  evidence: Raised by this spec's blind-hunter review layer; initially "fixed" by denying all institution=None non-superusers outright, but that broke users/tests/test_crud.py::UserDetailViewTest (3 pre-existing passing tests that rely on two institution=None users being able to view each other — confirmed this is accepted, exercised behavior, not an oversight, likely modeling the pre-Phase-2/single-tenant/not-yet-onboarded state). Reverted the outright-deny to preserve that intentional behavior. The narrower gap the reviewer actually meant (two accounts destined for two DIFFERENT future institutions, both still unassigned, seeing each other) needs a real product decision — e.g. treat "no institution yet" as globally admin-only-visible instead of peer-visible — not a mechanical fail-closed change; out of scope here since it would need its own spec and explicit sign-off given it changes tested behavior.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-gma-timeline-and-video-bookmark-mapping.md`
  summary: Test fixtures creating a full Patient instance (bht, baby_name, mother_name, gender, dob_tob, mo_delivery, pog_wks, pog_days, birth_weight, ofc, tp_mobile, added_by — 12 required-ish fields) are hand-duplicated across many test files (this spec alone touches three: ndas/tests/test_delete_helpers.py, patients/tests/test_models.py, patients/tests/test_timeline_utils.py) with no shared factory/helper
  evidence: Raised by this spec's blind-hunter review layer. Pre-existing pattern across the whole test suite (not introduced by this spec), but this spec's own new/edited fixtures add three more copies of it. A shared `make_test_patient(**overrides)` helper (e.g. in a new `patients/tests/factories.py`) would reduce duplication and the risk of a required-field change breaking dozens of hand-written fixtures at once — but introducing one now means touching many unrelated existing test files' imports to actually pay off, well beyond this spec's narrow bugfix scope.

- source_spec: `_bmad-output/implementation-artifacts/spec-fix-gma-timeline-and-video-bookmark-mapping.md`
  summary: patients/views.py's bookmark_view (~line 1542-1560) always treats bookmark.object_id as a Patient pk — `get_object_or_404(Patient.objects.for_institution(...), id=patient_id)` — regardless of `bookmark.bookmark_type`. For any non-Patient bookmark (Video/GMA/HINE/Attachment/DA/CDICR/GPA — 7 of the 8 mapped types), this either 404s outright or, worse, could coincidentally match an unrelated Patient sharing the same numeric id
  evidence: Discovered via this spec's second blind-hunter review pass while fixing bookmark_edit's redirect-on-orphan target (which had pointed at bookmark-view, itself broken for this reason — even for the Patient-type case, once the target Patient is deleted). Redirected bookmark_edit's orphan-check instead to bookmark-manager-user (safe, doesn't attempt object resolution). bookmark_view itself is unfixed and pre-existing — it apparently only ever really worked for Patient-type bookmarks; fixing it properly needs its own spec (route per bookmark_type to the right detail view/template, not just Patient's).
  RESOLVED by: direct fix (2026-09-01, no spec — small pinpointed follow-up) — bookmark_view (patients/views.py) now uses the generic `Bookmark.bookmarked_object_exists` property (routes by bookmark_type via MODEL_MAPPING) for its orphan-check instead of a hardcoded Patient lookup; on a missing target it redirects to bookmark-manager-user with a message, mirroring bookmark_edit's existing pattern. templates/bookmark/view.html already resolved the correct "Linked Resource" link per bookmark_type via its own `{% url %}` tags for 7 of 8 types — added the missing GPA branch. Each target view (patient_view, assessment_view, hine_assessment_view, attachment_view, da_assessment_view, cdic_assessment_view, gpa_view, video:view) already applies its own institution_scope() filter, so dropping the Patient-only gate does not weaken institution isolation. Regression tests added: patients/tests/test_bookmark_security.py::BookmarkViewRoutingTest (Patient bookmark unchanged, Video bookmark no longer 404s, orphaned bookmark redirects cleanly). Note: the model's separate `Bookmark.get_bookmarked_object_url()` helper (patients/models.py) was left untouched — it's dead code (no callers) with its own pre-existing bugs (wrong "file-view" url name for Video, wrong kwarg name for HINE/DA/CDICR/GPA which use hine_id/da_id/cdic_id/gpa_id not pk) — out of scope for this narrow view-routing fix.

- source_spec: none
  summary: Bookmark.get_bookmarked_object_url() (patients/models.py ~2378-2403) is unused dead code (no call sites anywhere in the repo) and, independent of that, is itself broken for 5 of its 8 mapped types — it maps "Video" to url name "file-view" (does not exist; the real name is the namespaced "video:view"), and passes kwargs={"pk": object_id} for HINE/DA/CDICR/GPA whose actual url patterns use hine_id/da_id/cdic_id/gpa_id, not pk. Every one of these mismatches is silently swallowed by the method's own `except NoReverseMatch` and returns None.
  evidence: Found while fixing the sibling bookmark_view routing bug (see entry immediately above) — considered reusing this helper instead of templates/bookmark/view.html's own per-type `{% url %}` tags, but the helper turned out to be both unused and broken. If this method is ever wired up (e.g. as a "quick jump" link elsewhere), it needs the same per-type kwarg-name fix already reflected correctly in templates/bookmark/view.html, plus either deletion (if truly dead) or a decision on where it should be called from.
