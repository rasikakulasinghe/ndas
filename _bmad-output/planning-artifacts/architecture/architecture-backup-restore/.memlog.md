---
scope: Institutional Admin & Super Admin Data Backup/Restore feature slice (spec-backup-restore) -- architecture additions: async execution mechanism, backup archive format and storage layout, manifest schema, restore access control, retention pruning trigger
purpose: Build-ready architecture spine for bmad-build, resolving SPEC.md's architecture-phase assumption and constraints into concrete invariants
altitude: feature slice of existing NDAS system; ratifies conventions from _bmad-output/planning-artifacts/architecture.md rather than replacing it
updated: 2026-09-17T18:30
---

- (note) Standing context from _bmad-output/planning-artifacts/architecture.md (ratified, not re-decided): Django 5.2 MVT server-rendered monolith, no REST/GraphQL API, HTMX partial updates only (no SPA/WebSockets), AdminLTE 3.2/Bootstrap 4.6 frozen, Gunicorn 3 workers with 300s timeout, WhiteNoise for static, manual deploys/no CI/CD automation, Redis already used in prod for caching via REDIS_URL, InstitutionScopedManager enforces tenant isolation, 14-layer middleware stack fixed order, django-ratelimit + python-magic MIME validation conventions, CSP nonce required on inline scripts
- (note) architecture.md's Implementation Handoff states 'No new development until Phase 2 is production-ready' -- flagged to user as standing guidance; user is actively directing this work so proceeding, not treated as a blocker
- (note) Brownfield precedent found: institution/views.py superadmin Excel export view uses FileResponse(open(path, 'rb'), content_type=...) with Content-Disposition attachment, from a permission-gated (superadmin-only) view, file generated outside any public media/static path -- direct precedent for the backup download endpoint
- (note) Brownfield precedent found: notification bell uses HTMX polling every 60 seconds against a Django view -- direct precedent for BackupJob progress polling
- (note) Brownfield precedent found: get_institution_video_path/get_institution_attachment_path in ndas/custom_codes/validators.py already partition media on disk as '{institution_slug}/videos/...' and '{institution_slug}/attachments/...' -- reused verbatim as the archive's internal media layout
- (decision) Async execution mechanism: user chose management command via detached subprocess over Celery+Redis. Binds: how backup/restore jobs execute outside the request/response cycle. Rule: a trigger view creates a BackupJob row, launches 'python manage.py run_backup <job_id>' (or run_restore) as a detached OS subprocess, and returns/redirects immediately -- never blocks the Gunicorn worker. Prevents: one implementer reaching for Celery and another for threading, producing two incompatible async patterns; also prevents a synchronous view exceeding Gunicorn's 300s timeout on multi-GB jobs. Rationale (memlog only): no automatic/scheduled triggering needed (spec non-goal), single-box manual-deploy team, matches architecture.md's deferred-PgBouncer/manual-CI-CD 'boring technology' bias -- Celery would add a new always-on worker process to deploy and monitor for marginal benefit here
- (decision) DB export mechanism: use django.core.serializers.serialize('json', queryset) per model against InstitutionScopedManager-scoped querysets in a dedicated backup-service module; never shell out to 'dumpdata'. Binds: how database rows are serialized into the archive. Prevents: an implementer using dumpdata (which dumps whole tables and cannot be scoped to one institution) and accidentally leaking cross-institution PHI into an institutional admin's backup
- (decision) Storage location: backup .zip files are written to a new BASE_DIR/backups/ directory, outside MEDIA_ROOT and STATIC_ROOT. Binds: where backup archives live on disk. Prevents: a backup ending up reachable through WhiteNoise static serving or the existing media-serving path, given archives are unencrypted at rest by spec decision
- (decision) Archive internal layout: manifest.json at the zip root; db/<app_label>.<model_name>.json one file per model; media/{institution_slug}/videos/... and media/{institution_slug}/attachments/... copied verbatim from the existing on-disk media layout (get_institution_video_path/get_institution_attachment_path). Binds: the zip's internal structure. Prevents: backup-writing code and restore-reading code drifting on where each piece lives, and keeps the human-browsable requirement concrete and testable
- (decision) Manifest schema (manifest.json): schema_version (from Django migration state or an NDAS app version string), institutions (list of institution slugs included), record_counts (dict of model -> row count), checksums (dict of archive-relative path -> SHA-256), generated_at, generated_by (user id/username). Binds: the manifest contract CAP-2 depends on. Prevents: backup-writer and restore-validator code disagreeing on what fields the manifest carries
- (decision) Download/restore-upload endpoint: reuse the FileResponse(open(path, 'rb'), content_type=...) + Content-Disposition attachment + permission-gated-view pattern already used by institution/views.py's superadmin Excel report export. Binds: how the backup .zip is streamed to the browser on download. Prevents: inventing a second, inconsistent file-serving mechanism when a working, reviewed precedent already exists in this codebase
- (decision) Progress/status surface: a BackupJob model (status, progress_pct, error_message, created_at, updated_at, triggered_by FK to user, scope) is polled via HTMX on a job-status partial, using the same polling mechanism as the existing 60-second notification-bell poll (interval may be shorter, e.g. 3-5s, since this is an actively-watched job rather than a passive badge). Binds: how job progress reaches the UI. Prevents: a second, incompatible real-time mechanism (e.g. WebSockets) being introduced, which architecture.md's API & Communication Patterns table rules out
- (decision) Retention pruning trigger: lazy/opportunistic pruning on access -- the backup-list view, when the (disabled-by-default) age-based retention policy is enabled, first deletes any backup past the configured max-age, then renders the remaining list. No OS cron entry or Celery beat schedule is introduced. Binds: how and when old backups are actually deleted. Prevents: a second implementer adding a cron job or scheduled task for pruning that duplicates or races with a simpler in-request check, and avoids introducing any periodic-execution infrastructure this project does not otherwise have
- (decision) Authorization boundary: all backup/restore views reuse the institution/ app's existing tenant-scoping (middleware.py, context_processors.py) and existing role-check patterns; restore views additionally require an explicit super-admin-only check per SPEC.md CAP-3/CAP-4. Binds: how every backup/restore view authorizes a request. Prevents: a parallel permission-check mechanism that could diverge from the canonical institution-scoping logic used everywhere else in Phase 2
- (decision) Request/response flow for triggering a job: the trigger view (backup-now / restore-now) must create the BackupJob row, launch the detached subprocess, and return an HTTP redirect to the job-status page within the same request -- it must never wait on the subprocess to finish. Binds: the trigger endpoint's synchronous behavior. Prevents: a synchronous implementation that blocks a Gunicorn worker for the duration of a multi-GB backup/restore, given the existing 300s Gunicorn timeout
- (decision) Abuse/trust boundary: backup and restore trigger endpoints are rate-limited via the existing django_ratelimit pattern (key=user_or_ip); an uploaded restore .zip is validated via the same python-magic MIME-type check used for other file uploads before its contents are trusted, followed by CAP-2's manifest/checksum validation before any data is touched. Binds: the abuse-prevention and upload-trust boundary for this feature. Prevents: an implementer treating the restore upload as pre-trusted because it 'came from an admin'
- (decision) PHI protection without encryption: since SPEC.md's non-goals explicitly exclude encryption at rest, the BASE_DIR/backups/ storage-location decision and the permission-gated download/restore-upload endpoint together are the complete compensating control -- no implementer may add partial/ad-hoc encryption later without a spec update, since that would silently contradict the human-browsability requirement (CAP-1, CAP-2 constraints)
- (decision) Deployment/environment envelope: the subprocess is launched as [sys.executable, path-to-manage.py, 'run_backup'|'run_restore', str(job_id)] via subprocess.Popen, never a shell script or OS-specific command, so it runs identically on Windows dev and Linux prod -- matches architecture.md's 'deployment flexibility: on-premise or cloud, same codebase, no architecture changes between targets'. No new service, process manager, or deployment step is introduced; the existing Gunicorn/Nginx/WhiteNoise stack is untouched
- (direction) Deferred: stuck/orphaned BackupJob detection (a subprocess that dies mid-job leaves a job row stuck at 'running'). No monitoring infra exists anywhere in this project yet (architecture.md names uptime monitoring as a Nice-to-Have Gap). Cheap mitigation without new infra: the job-status view flags a job as stale if updated_at hasn't advanced in N minutes; a real watchdog/reaper is deferred until this project has monitoring at all
- (event) Coaching conversation complete: async mechanism (subprocess), export mechanism, storage location, zip layout, manifest schema, download pattern, progress polling, retention trigger, authorization boundary, request flow, abuse/trust boundary, and deployment envelope all decided. Proceeding to distill ARCHITECTURE-SPINE.md
- (event) Distill complete: ARCHITECTURE-SPINE.md written with 12 ADs (AD-1..AD-12), consistency conventions, stack (no new deps), structural seed, capability map, and deferred list. Reconciliation subagents dispatched against SPEC.md and architecture.md before the Reviewer Gate
- (event) Reconciliation vs architecture.md: PASS on all 5 checked standing invariants (no-API/HTMX-only, middleware order, Gunicorn timeout, MIME validation, InstitutionScopedManager reuse). One completeness gap found: the spine does not carry architecture.md's 'No new development until Phase 2 is production-ready' sequencing caveat anywhere -- will add a one-line caveat note
- (decision) CORRECTION to AD-3, independently verified against patients/models.py, video/models.py, problemlist/models.py, referral/models.py: InstitutionScopedManager only exists on Patient (via PatientManager) and the 3 referral/ models. Video uses plain VideoManager(models.Manager) with no scoping; Attachment, GMAssessment, HINEAssessment, DevelopmentalAssessment, Problem use Django's default manager, no scoping. All are reachable only via patient FK. Corrected rule: scope each exported model via its actual FK path -- InstitutionScopedManager.for_institution() for Patient/referral models only; .filter(patient__institution=institution) for Video/Attachment/GMAssessment/HINEAssessment/DevelopmentalAssessment/Problem; .filter(problem__patient__institution=institution) for ProblemAction. Original AD-3 text was factually wrong and would have misled an implementer into a false uniform-scoping assumption -- real cross-institution PHI leak risk if built as originally written
- (event) SPEC.md reconciliation report received (no files edited by the fork): confirmed AD-3 scoping error (matches independent verification); 4 additional gaps found -- streaming/chunked constraint unaddressed by any AD, CAP-3 pre-restore snapshot mechanism ungoverned, CAP-6 manual per-backup delete ungoverned, CAP-7 audit trail weakened to plain log files instead of SPEC's named UserActivityMiddleware/UserTrackingMixin mechanism
- (decision) Fix: BackupJob and BackupRetentionPolicy models must inherit TimeStampedModel+UserTrackingMixin per the project's mandatory model pattern -- this was omitted from AD-1/AD-8 and is exactly what CAP-7's audit trail needs (added_by/created_at capture who-triggered-when for backup creation and restore, structurally, not via log-file parsing)
- (decision) Fix: streaming/chunked I/O is now explicit -- AD-3's serializer export writes directly into an open zipfile.ZipFile handle per model (never buffers the whole DB export in memory); media files are copied into the zip via chunked shutil.copyfileobj with a bounded buffer. Satisfies SPEC.md's 'no artificial dataset-size cap' constraint, previously unaddressed by any AD
- (decision) Fix: pre-restore snapshot (CAP-3) reuses the CAP-1 backup-creation code path rather than inventing new snapshot machinery -- run_restore.py's first step is to call the same backup service to create a normal backup of current state, store it in BASE_DIR/backups/ like any other backup, and link it via BackupJob.pre_restore_snapshot (self-referential FK) before applying any restore changes. 'Returning to prior state' means the super admin restores from that snapshot through the normal restore flow -- no separate snapshot format or rollback mechanism needed
- (decision) Fix: manual per-backup deletion (CAP-6) gets its own view in backup/views.py -- permission-gated per scope (institutional admin: own institution's backups only; super admin: any), removes the .zip from BASE_DIR/backups/ and the BackupJob row. Distinct from AD-9's policy-driven auto-prune
- (decision) Fix: audit trail split by action type -- backup/restore CREATION is captured structurally via BackupJob's UserTrackingMixin fields (added_by, created_at), matching CAP-7's named mechanism exactly; DOWNLOAD is a GET request with no natural model mutation to hang UserTrackingMixin off, so it is explicitly logged to the existing logs/security.log via the standard logger convention, consistent with architecture.md's 'security events to dedicated log' rule. This is a considered split, not a silent weakening
- (event) All 5 reconciliation findings fixed in ARCHITECTURE-SPINE.md (AD-3 rewritten, new AD-13/AD-14/AD-15 added, AD-1/AD-8 updated for UserTrackingMixin inheritance, Consistency Conventions Logging row corrected). Re-running lint before the remaining reviewer-gate lenses
- (event) Reviewer Gate complete: 3 reviews received (rubric-walker, version-verification, adversarial-divergence), all written to reviews/ subfolder. ~15 additional real findings beyond the already-fixed AD-3 scoping error. Triaged: all but one are clear single-correct-answer fixes, applied directly below; one (restore-vs-concurrent-live-writes) was a genuine product trade-off, asked the user
- (decision) User decision: job-level lock only for restore concurrency (no system-wide maintenance mode). Trigger view for backup/restore refuses to start if another BackupJob for the same scope is already pending/running. Residual risk (an ordinary clinician save colliding with an in-progress restore of the same records) accepted given restores are rare, admin-triggered, brief-ish events -- this is new AD-16
- (decision) Fix (subprocess detachment, rubric H3 + version review #3): AD-2 must require start_new_session=True on POSIX and creationflags=DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP on Windows, so the child survives Gunicorn worker recycling/restart -- the whole stated purpose of AD-2. Also: stdout/stderr must be redirected to files (e.g. BASE_DIR/backups/<job_id>/job.log), never left as PIPE (deadlocks once the OS pipe buffer fills on a long-running chatty process) and never inherited from the parent worker's fds
- (decision) Fix (F5, checksum format + manifest self-integrity): checksum dict keys in manifest.json use forward-slash POSIX-style paths matching zipfile.namelist() output exactly, regardless of build OS. Chicken-and-egg (manifest can't hash itself) resolved by NOT trying: the whole-archive SHA-256 is computed after the zip is finalized and stored in BackupJob.archive_checksum (a DB field, trusted/non-user-controlled), not inside the zip
- (decision) Fix (F6 + version review #2, schema_version shape/semantics): schema_version is a single sha256 hex string over sorted 'app_label.migration_name' tuples from MigrationRecorder.applied_migrations(). Comparison is exact equality (conservative/safe default matching this project's boring-technology bias) -- a restore whose schema_version differs at all from the current DB's is rejected with a clear error naming the mismatch; not superset/subset/per-app-relevant, to avoid a restore silently accepting a schema drift it can't actually reconcile. Can be relaxed later via a spec update if this proves too brittle in practice
- (decision) Fix (F7, multi-institution restore scoping): clarified restore is always whole-archive and atomic to the scope it was created for (already stated as a non-goal: no cross-institution merging on restore) -- there is no partial/per-institution restore-time selection out of a multi-institution super-admin archive. This means AD-5's one-file-per-model (union across selected institutions) is sufficient as written; no institution-namespaced path or row-level institution tag is needed for restore purposes. Per-institution record_counts in the manifest (CAP-2) are computed via separate scoped COUNT queries at backup-creation time, not derived from row tags
- (decision) Fix (restore semantics / PK handling, version review #1): restore is delete-scoped-then-reload, not merge. For each model in a fixed dependency order (Patient first; then Video, Attachment, GMAssessment, HINEAssessment, DevelopmentalAssessment, Problem; then ProblemAction last), inside one transaction per model: delete existing rows matching the archive's institution scope, then deserialize and save the archive's rows for that model with their original PKs preserved. Deleting-then-recreating with the same PKs avoids the auto-increment sequence collision the reviewer flagged, since no new PK space is being claimed. Institution rows themselves are never recreated by restore -- the target institution(s) must already exist in the destination DB (identified by slug), consistent with restore always happening on the same live system, never a fresh empty DB
- (decision) Fix (F1, pre-restore snapshot ownership): the snapshot IS a first-class BackupJob row (job_type=pre_restore_snapshot), stored/downloadable exactly like any other backup (AD-4/AD-5/AD-7) -- but is EXEMPT from AD-9's age-based auto-pruning entirely (never auto-deleted; only removable via AD-15's manual super-admin delete), since it is the one rollback path for a restore. Visibility follows the same AD-10 restore-is-super-admin-only rule -- only super admins see/download pre-restore snapshots, consistent with only super admins being able to trigger restores in the first place
- (decision) Fix (F2, prune races with in-use backup): AD-9's prune query excludes any backup that is the source of a BackupJob currently pending or running (covers both an in-progress restore reading it and an in-progress snapshot/backup job referencing it)
- (decision) Fix (F3, terminal status ownership): the trigger view is responsible for writing BackupJob.status=failed (with an error_message) if subprocess.Popen itself raises before the command process ever starts -- this is the one case a view is allowed to write a status transition, as an explicit exception to 'only the command process writes status'. Pending/running rows never count toward AD-9's age-based pruning (age is measured from completed_at, not created_at) -- a job that hasn't finished can't be 'old' in the pruning sense
- (decision) Fix (F4, atomic status/progress): BackupJob.status is the sole authoritative completion signal; the terminal progress_pct=100 write and status=completed write happen together in one .save(update_fields=[...]) call. Consumers (status partial, notification) must never treat progress_pct alone as a completion signal
- (decision) Fix (F8, prune-delete query scope + attribution): the age-based prune sweep only runs when a SUPER ADMIN loads a backup-list view (never on an institutional admin's page load -- an institutional admin never triggers deletion of data outside their own scope). The delete query itself is system-wide (matching CAP-6's 'system-wide... policy'), consistent with only the role authorized to configure the policy being the one whose page load can execute it. The prune action is logged as a system-attributed entry ('system: pruned N backups older than Xd'), not fabricated as any individual user's triggered_by
- (decision) Fix (F9, single-entry-point enforcement): AD-1's Rule now explicitly states BackupJob creation and backup/services.py's trigger-level functions may only be called from backup/views.py -- another app (e.g. institution/) needing a backup entry point must link/redirect to backup/'s own views, never import services.py directly. Makes the app-boundary intent an enforced rule, not just Prevents-column prose
- (decision) Fix (F10, download status precondition): AD-7's download view requires BackupJob.status=='completed' (get_object_or_404 filtered on status) before attempting FileResponse; a vanished/not-yet-ready file returns a specific 'not available' error, never an unhandled exception or a partial/corrupt zip served as if valid
- (decision) Fix (H4, CAP-5 notification-after-navigating-away): reuses the existing referral app's Notification model/bell pattern (already polled every 60s per architecture.md) -- run_backup.py/run_restore.py creates a Notification row on completion or failure, so the admin is notified regardless of which page they're on. professional-options.md's 'email notification' remains a distinct, larger beyond-v1 candidate, not a duplicate of this
- (decision) Fix (M1, BackupJob fields): add job_type field (backup|restore|pre_restore_snapshot) as the backup/restore discriminator CAP-6/CAP-7 need; size is not a stored field, computed via os.path.getsize() at render time (stated explicitly, not left implicit)
- (decision) Fix (M2, BackupJob.scope shape): scope is an explicit nullable Institution FK (null = system-wide, set by a super admin's system-wide backup), not a bare untyped field -- matches the project's canonical scoping pattern architecture.md requires elsewhere
- (decision) Fix (M3, disk-capacity risk): before starting a job, check shutil.disk_usage(BASE_DIR) against a safety margin (estimated job size * 1.2, or a flat minimum-free-space guard if estimation isn't feasible); insufficient space refuses to start and sets status=failed immediately with a clear error, rather than failing mid-write
- (decision) Fix (L1, audit-trail framing correction): corrected the spine's own language -- BackupJob.triggered_by is set explicitly by the view (request.user) when creating the row, matching the real, code-verified pattern used everywhere else in this codebase (referral/views.py, video/views.py all set added_by manually), NOT an automatic UserActivityMiddleware behavior despite how CLAUDE.md's shorthand describes it. This avoids an implementer assuming triggered_by is populated 'for free' and skipping it
- (decision) Fix (L2, tests convention): Structural Seed now includes backup/tests/ as a package (never a bare tests.py, per CLAUDE.md's explicit project-wide warning about that exact collision)
- (event) architecture.md itself is stale on its own Django version claim (says 4.2.16 in 4 places; requirements.txt and CLAUDE.md both confirm the real version is Django 5.2 LTS) -- out of scope for this spine to fix, flagging back to the user/whoever owns that document
- (event) All fixes applied to ARCHITECTURE-SPINE.md: AD-1/AD-2/AD-3/AD-5/AD-6/AD-7/AD-8/AD-9/AD-14 revised, AD-16/AD-17 added, Consistency Conventions and Capability Map updated, Deferred section carries the two out-of-scope-but-flagged notes (maintenance-mode decline, architecture.md's stale Django version claim). Lint re-run clean (3 false-positive placeholder warnings on legitimate path-template tokens, no structural issues). Manual full re-read confirms internal coherence. Spine now 17 ADs total. status: final. spine finalized
- (direction) SPEC.md updated (CAP-1/CAP-2/CAP-3 revised): DB export must be a SINGLE consolidated JSON file (not per-model files), plus a new optional date/date-range export filter anchored on Patient.created_at that cascades to every related record of a qualifying patient, plus restore must support both full-scope and date-scoped (partial) archives with patient-level skip-on-conflict semantics. Architecture spine must be reconciled
- (decision) AD-5 amended in place: archive layout changes from db/<app_label>.<model_name>.json (one file per model) to a single db_export.json. Binds/Prevents unchanged
- (decision) AD-3 amended in place: writer-side export now streams into ONE db_export.json -- a single JSON object keyed by app_label.model_name, each value a JSON array -- instead of one zip entry per model. Still processes one model's queryset at a time via .iterator(), writing each serialized record individually (json.dumps of a single-object 'python' serialization) rather than accumulating a per-model list, so AD-13's no-full-buffering discipline now holds at the per-record level, not just per-model. AD-3's existing delete-scoped-then-reload restore semantics (preserve original PKs) is scoped explicitly to FULL-scope (no date filter) restores only; date-scoped (partial) restores follow the new AD-18/AD-19 path instead
- (decision) AD-6 amended in place: manifest.json gains date_filter: {applied: bool, start: date|null, end: date|null, field: 'patient.created_at'} so restore can validate and preview a date-scoped archive's declared scope before applying
- (decision) New AD-18: date-scoped export cascade query. When a date/date-range filter is supplied, resolve patient_qs = Patient.objects.for_institution(institution).filter(created_at__date range). Every other in-scope model (Video, Attachment, GMAssessment, HINEAssessment, DevelopmentalAssessment, Problem) is filtered by .filter(patient__in=patient_qs) instead of AD-3's institution-only filter; ProblemAction by .filter(problem__patient__in=patient_qs). No date filter present -> AD-3's original institution-only filters apply unchanged (full scope). record_counts in the manifest are computed against these same (possibly narrowed) querysets
- (decision) New AD-19: date-scoped (partial) restore is ADDITIVE-ONLY with PK remapping, never delete-then-reload. Step 1: read the Patient section of db_export.json; for each archived patient row, attempt to match an existing target-DB patient using whichever of bht/nnc_no/ptc_no/pc_no that row has populated (first non-null match wins; all are unique-if-set on Patient). Step 2: partition into skip-set (matched) and import-set (unmatched); a patient with none of the four identifiers set cannot be matched and always falls into the import-set (accepted residual risk of an eventual duplicate for identifier-less patients -- mirrors AD-16's accepted-residual-risk pattern; flagged to user). Step 3: inside one DB transaction for the whole apply step (all-or-nothing), for each import-set patient in dependency order (Patient first, then Video/Attachment/GMAssessment/HINEAssessment/DevelopmentalAssessment/Problem, then ProblemAction last): set pk=None on deserialization so Django assigns a new PK, save, and record source_pk->new_pk in an in-memory mapping table; every FK field on a dependent row (patient, or problem for ProblemAction) is remapped through that table before saving. Skip-set patients and every one of their related rows (identified by fields.patient / fields.problem-chain equal to a skipped source PK) are not touched at all. Step 4: the restore preview (before the admin confirms) lists which patients will be skipped vs imported, resolved via Step 1-2 without yet applying anything
- (decision) AD-14 (pre-restore snapshot) applies to date-scoped restores too, for defense-in-depth/post-hoc undo, even though AD-19's transaction already makes a failed partial-restore atomic (no half-imported patient state possible without a snapshot)
- (decision) AD-19 clarified: the AD-14 pre-restore snapshot taken ahead of a date-scoped restore is always a full-scope (undated) backup of the target institution(s), never itself date-filtered, so it can fully reverse the additive import if the admin needs to undo it post-hoc
- (note) Reviewer Gate: data-integrity lens (FAIL) + adversarial lens (FAIL) both returned before Finalize. Verified against repo and consolidating into one fix pass rather than patching piecemeal: (1) GMAssessment.video_file OneToOneField->Video CASCADE not in AD-19's remap table [both reviewers, code-verified], (2) Patient.institution never remapped on import -- source institution PK kept verbatim, cross-institution leak risk [both reviewers, code-verified], (3) Patient.pin is a 5th unique=True,null=True identifier missing from AD-19's match logic (adversarial, code-verified at patients/models.py:192) -- match set is bht/nnc_no/ptc_no/pc_no/pin, all five, any-field-matches, (4) UserTrackingMixin added_by/last_edit_by + ProblemAction.performed_by (all FK->CustomUser, nullable/SET_NULL, code-verified) never remapped -- preserve if target PK exists else null, (5) GMAssessment.diagnosis M2M->DiagnosisList treated as shared global reference data, preserved as-is not remapped, (6) cascade model list missing CDICRecord and GeneralPaediatricAssessment (both real patient FK, code-verified) -- add to both AD-3 and AD-18/19's dependency order, (7) referral app's 3 models excluded from date-scoped cascade entirely -- ReferralReceived/ReferralMessage deliberately have no patient FK (self-contained cross-institution design, code-verified in referral/models.py), referral stays full-scope-only per SPEC.md's cross-institution non-goal, (8) Bookmark model (generic type/object_id reference, not a direct patient FK) is a pre-existing gap predating this change (also affects original full-scope AD-3) -- deferred, not solved now, flagged explicitly rather than silently dropped, (9) media restore step must be driven by the import-set patients' Video/Attachment file references, never a blind copy of the zip's media/ folder, so a skipped patient's media is never written to disk, (10) AD-19's single whole-archive transaction changed to one transaction PER import-set patient -- resolves the AD-8 progress-visibility and AD-16/SQLite-lock tension the adversarial reviewer raised, and is intentional since patients are independent units: a failed patient rolls back alone, already-committed patients before it stay imported, (11) ambiguous-match guard added both directions -- one archived patient matching multiple distinct target patients, or multiple archived patients matching the same target patient -- rejects/skips the ambiguous patient(s) with a logged error rather than guessing, (12) multi-institution query gap: for_institution() only takes one institution or None (all) per institution/managers.py -- AD-18 (and by extension AD-3) now names the three-way branch explicitly: single institution via for_institution(inst), an explicit multi-institution subset via .filter(institution__in=selected), system-wide via all_institutions(), (13) db_export.json must always include every cascade model's key even as an empty array -- never omit a key for zero matches, removing a reader/writer ambiguity
- (note) Re-verification reviewer: PASS WITH NOTES. All 10 fixes confirmed landed correctly. Two sequencing gaps found and fixed: (A) institution-slug resolution moved from step 4 into step 1 (Match) so a patient's skip/import/excluded outcome is fully and immutably resolved before the preview is shown -- previously a preview-confirmed patient could still be rejected later during the transactional import step, breaking the preview's promise; (B) media-application note now explicitly keys off this same final step-1 partition (import-set only, after institution and ambiguous-conflict exclusions), so a patient excluded for a missing-institution reason can no longer have orphaned media written to disk. Also fixed a minor cosmetic gap: AD-3's referral-exclusion bullet now explicitly justifies excluding ReferralSent too (it does have a patient FK, but is excluded alongside ReferralReceived/ReferralMessage as a set so one referral thread never splits across archive types)
- (event) Reviewer Gate complete for this update: version-check PASS WITH NOTES (env drift note only, no fix needed), data-integrity FAIL then fixed, adversarial FAIL then fixed, re-verification PASS WITH NOTES then two sequencing gaps fixed. lint_spine.py clean (only pre-existing low-severity false-positive placeholder warnings, unchanged from before this update). Spine updated: AD-3 (amended: single-file streaming, institution_scope 3-way branch, CDICRecord/GeneralPaediatricAssessment added, referral-3-models explicitly excluded from date-scoped cascade, Bookmark gap named), AD-5 (amended: single db_export.json, always-present keys), AD-6 (amended: manifest date_filter field), AD-18 (new: date-scoped cascade query), AD-19 (new: date-scoped partial restore -- 5-identifier match with ambiguous-conflict guard, institution slug-remap resolved up front in Match step, video_file/added_by/last_edit_by/performed_by FK remap, per-patient transactions, media application keyed to final partition). Frontmatter updated date set to 2026-09-17
