/**
 * Main JavaScript file for NDAS application
 * Bootstrap components are initialized by app-utils.js
 * Video.js initialization is handled by video-manager.js
 */

// Verify jQuery is loaded
if (typeof $ === 'undefined') {
    console.error('❌ jQuery not available in main.js');
}
